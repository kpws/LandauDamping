gmp must first be installed for pgs. To compile run:
gcc pgs.c -pthread -lgmp -lm -o pgs

fftw must first be installed for cgs. To compile run:
gcc cgs.c -pthread -lfftw3_threads -lfftw3 -lm -o cgs

Cheers,
Petter